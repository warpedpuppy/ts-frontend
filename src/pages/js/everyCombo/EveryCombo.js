import React from 'react'

export default function EveryCombo() {

    // function EveryComboOfArray(arr) {
    //     let offset, endOfStartArray = 1, goal = arr.length - 1, matches = [];
    //     while (arr.length) {
    //         while (endOfStartArray < arr.length) {
    //             let root = [arr[0], arr[endOfStartArray]];
    //             offset = endOfStartArray + 1
    //             while (offset <=  arr.length) {
    //                 let stem = arr.slice(offset)
    //                 let temp = [...root, ...stem]
    //                 offset ++;
    //                 console.log(temp)
                    
    //             }
    //             endOfStartArray ++;     
    //         }
    //         arr.splice(0, 1);
    //         offset = 1, endOfStartArray = 1;
    //     }
       
    //    // return matches.sort( (a,b) => a.length - b.length)[0].length;
    // }
    return (
        <div>
            
        </div>
    )
}
